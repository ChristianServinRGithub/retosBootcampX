const header = document.querySelectorAll(".header");
const botonInicio = document.querySelectorAll(".nav");
const cards = document.querySelectorAll(".cards");
const footer = document.querySelectorAll(".footer");
